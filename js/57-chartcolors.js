/* chartColors: one application-level function per file. */
function chartColors(){
  return { accent: cssVar('--accent'), teal: cssVar('--teal'), text: cssVar('--text-dim'),
           grid: cssVar('--border'), weekend: cssVar('--weekend'), workday: cssVar('--workday'),
           surface: cssVar('--surface') };
}
