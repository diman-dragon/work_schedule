/* utils/dollar-helper.js
 * Короткий алиас для document.getElementById — используется в нескольких
 * небольших модулях UX-улучшений (навигация по месяцам, печать, бэкап и т.д.),
 * которые раньше были одним общим IIFE (initEnhancements) с локальным `$`.
 * При разбиении на атомарные файлы вынесен сюда как общий глобальный хелпер;
 * этот файл должен подключаться раньше файлов, которые используют `$(...)`.
 */
function $(id){ return document.getElementById(id); }
